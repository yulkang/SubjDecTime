classdef RandStr
methods (Static)
    function seedStr(siz, r)
        % Generate 5-randStr which can be converted to a random seed.
    end
    function randStr(siz, n, r)
        % Generate randStr.
        if nargin == 0
        end
    end
    function v = s2v(s)
        % Convert a randStr to an integer.
    end
    function s = v2s(v)
        % Convert an integer to a randStr.
    end
end
end